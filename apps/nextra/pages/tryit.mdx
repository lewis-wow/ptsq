import { searchParams } from '../utils/searchParams';

<section style={{ height: '100%' }}>
  <iframe
    src={`https://stackblitz.com/github/lewis-wow/ptsq/tree/master/examples/basic?${searchParams(
      {
        embed: '1',
        file: ['src/server.ts', 'src/client.ts'],
        hideNavigation: '1',
        terminalHeight: '1',
        showSidebar: '0',
        view: 'editor',
      },
    )}`}
    style={{
      width: '90%',
      height: 'auto',
      margin: '10px auto',
      aspectRatio: '16 / 9',
    }}
  />
</section>

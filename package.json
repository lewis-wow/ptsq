{
  "name": "bakalarska_prace",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "test": "vitest"
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "tsconfig": "*",
    "turbo": "latest",
    "vitest": "^0.34.5"
  },
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "packages/*"
  ]
}
